# Información del entorno de compilación

Para la compilación del programa se utilizó el compilador **GCC (GNU Compiler Collection)**.

La versión del compilador es:
gcc (GCC) 14.1.0

Asimismo, el compilador implementa el estándar **C23**, lo cual se verifica mediante la macro predefinida:

__STDC_VERSION__ = 202311L
